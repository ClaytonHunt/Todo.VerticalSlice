<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:ToDo.Mobile.ViewModels;assembly=ToDo.Mobile"
             x:Class="ToDo.Mobile.Views.ItemSummaryView">
    <ContentView.BindingContext>
        <viewModels:ItemSummaryViewModel />
    </ContentView.BindingContext>
    <ContentView.Content>
      <StackLayout Orientation="Horizontal">
          <CheckBox Color="{StaticResource Primary}" IsChecked="{Binding Item.IsCompleted}">
          </CheckBox>
          <Label Text="{Binding Item.Task}"
                 LineBreakMode="NoWrap" 
                 VerticalTextAlignment="Center"
                 Style="{DynamicResource ListItemTextStyle}" 
                 FontSize="16" />
      </StackLayout>
  </ContentView.Content>
</ContentView>