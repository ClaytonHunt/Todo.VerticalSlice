<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:behaviors="clr-namespace:ToDo.Mobile.Behaviors;assembly=ToDo.Mobile"
             x:Class="ToDo.Mobile.Views.ItemSummaryView">
  <ContentView.Content>
      <StackLayout Orientation="Horizontal">
          <CheckBox Color="{StaticResource Primary}" IsChecked="{Binding IsCompleted, Mode=OneWay}">
              <CheckBox.Behaviors>
                  <behaviors:EventToCommandBehavior 
                      EventName="CheckedChanged"
                      Command="{Binding CompleteItem}"
                      CommandParameter="{Binding Item}">
                  </behaviors:EventToCommandBehavior>
              </CheckBox.Behaviors>
          </CheckBox>
          <Label Text="{Binding Task}" 
                 LineBreakMode="NoWrap" 
                 VerticalTextAlignment="Center"
                 Style="{DynamicResource ListItemTextStyle}" 
                 FontSize="16" />
      </StackLayout>
  </ContentView.Content>
</ContentView>